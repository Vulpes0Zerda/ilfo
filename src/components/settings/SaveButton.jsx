const SaveButton = () => {
    return (
        <button
            type="submit"
            className="settings__bar__save"
            onClick="updateSettings()"
        >
            Save
        </button>
    )
}

export default SaveButton
